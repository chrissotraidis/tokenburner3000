# Referee

## What It Does
After the fight ends, a scoring engine evaluates both fighters' performance across 5 categories based on token volume, cost burned, style, and efficiency. A dramatic verdict statement is displayed with the winner announcement.

## Why It Matters
The verdict is the most shareable moment. The scoring system creates meaningful outcomes where expensive verbose models and cheap efficient models win in different categories.

## Core Rules
- Scoring is computed by `src/lib/scoring.ts` after the fight ends (implemented)
- 5 scoring categories, each 1-10 points (50 total) (spec-stated, implemented):
  - **Verbal Devastation** — Raw token volume: higher output = higher score
  - **Theatrical Commitment** — Money burned: more expensive output = higher score
  - **Creative Strategy** — Style bonus: model personality affects score
  - **Token Efficiency Ratio** — Tokens per dollar: cheap fast models score higher
  - **Main Character Energy** — Partially random crowd factor, biased toward output volume
- Verdict statement is randomly selected from 7 pre-written templates that reference fighter names and costs (implemented)
- Dramatic 1.5-second pause before verdict display (spec-stated, implemented)
- Arena goes to "Judging..." screen during the pause (implemented)
- Winner determined by higher total score; ties go to fighter 1 (implemented)
- Per-fighter cost breakdown shown in the compute receipt (implemented)

## Scoring Design Intent
- **Expensive verbose models** (Claude Opus, Grok) tend to win Devastation + Commitment
- **Cheap fast models** (Llama, DeepSeek V3) tend to win Efficiency
- **Creativity** adds personality-driven variance
- **Main Character Energy** adds randomness to prevent fully deterministic outcomes
- No fight outcome is guaranteed — there is always a chance the underdog wins

## What's Assumed
- V1 uses algorithmic scoring from fight metrics. Future versions may use a real referee model API call. Risk if wrong: Low
- Verdict statements are templates, not generated by AI. Risk if wrong: Low

## Key References
- **Source spec:** tokenburner-3000-concept-v2.md, section "4. THE SCORING"
- **Implementation:** src/lib/scoring.ts (scoring engine), src/App.tsx (verdict generation), src/components/Verdict.tsx (display)

## Acceptance Criteria
- [x] Scoring computed after fight ends based on token volume and cost
- [x] Returns scores across all 5 categories (1-10 each)
- [x] Dramatic pause before verdict display
- [x] Written verdict statement displayed prominently
- [x] Winner declared based on total score
- [x] Scores displayed in side-by-side comparison with fighter labels
- [x] Per-fighter cost breakdown in receipt

## Status
🟢 Implemented (mock scoring — algorithmic, not AI-generated)
